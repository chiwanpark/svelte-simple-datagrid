---
'@chiwanpark/svelte-simple-datagrid': minor
---

Fix theme overrides losing to the built-in CSS custom properties by declaring the defaults inside `:where()`, and add a `style` prop to set the variables inline.
