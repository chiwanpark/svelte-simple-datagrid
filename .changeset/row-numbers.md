---
'@chiwanpark/svelte-simple-datagrid': minor
---

Add a `rowNumbers` option that shows an auto-generated row number column with whole-row selection, like AG Grid, and a `rowNumbers` export option for CSV and XLSX.
