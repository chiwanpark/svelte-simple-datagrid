---
'@chiwanpark/svelte-simple-datagrid': minor
---

Add an `autoSize` prop and an `autoSizeColumns(columnIds?, { skipHeader, maxWidth })` method that size columns to fit their content, so wide grids scroll horizontally instead of squeezing every column.
